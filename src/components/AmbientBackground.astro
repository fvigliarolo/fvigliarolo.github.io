<div class="ambient-background" aria-hidden="true">
	<div class="mesh mesh-one"></div>
	<div class="mesh mesh-two"></div>
	<div class="mesh mesh-three"></div>
	<div class="orb orb-one"></div>
	<div class="orb orb-two"></div>
	<div class="orb orb-three"></div>
	<div class="grid"></div>
	<div class="glow-ring"></div>
	<div class="particle-field">
		<span class="particle particle-one"></span>
		<span class="particle particle-two"></span>
		<span class="particle particle-three"></span>
		<span class="particle particle-four"></span>
		<span class="particle particle-five"></span>
		<span class="particle particle-six"></span>
	</div>
</div>

<style>
	.ambient-background {
		position: fixed;
		inset: 0;
		overflow: hidden;
		pointer-events: none;
		z-index: 0;
	}

	.mesh,
	.orb,
	.grid,
	.glow-ring,
	.particle {
		position: absolute;
	}

	.mesh {
		filter: blur(90px);
		opacity: 0.48;
		border-radius: 999px;
		mix-blend-mode: screen;
	}

	.mesh-one {
		width: 40rem;
		height: 40rem;
		top: -8rem;
		left: -10rem;
		background: radial-gradient(circle, rgba(80, 203, 199, 0.24) 0%, rgba(80, 203, 199, 0) 72%);
		animation: driftOne 18s ease-in-out infinite alternate;
	}

	.mesh-two {
		width: 34rem;
		height: 34rem;
		top: 18%;
		right: -10rem;
		background: radial-gradient(circle, rgba(77, 124, 255, 0.22) 0%, rgba(77, 124, 255, 0) 70%);
		animation: driftTwo 22s ease-in-out infinite alternate;
	}

	.mesh-three {
		width: 28rem;
		height: 28rem;
		bottom: -8rem;
		left: 28%;
		background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, rgba(255, 255, 255, 0) 70%);
		animation: driftThree 20s ease-in-out infinite alternate;
	}

	.orb {
		border-radius: 50%;
		border: 1px solid rgba(216, 216, 216, 0.08);
		background: radial-gradient(circle at 30% 30%, rgba(255, 255, 255, 0.18), rgba(255, 255, 255, 0.02) 45%, rgba(255, 255, 255, 0) 70%);
		box-shadow: inset 0 0 30px rgba(255, 255, 255, 0.05);
	}

	.orb-one {
		width: 18rem;
		height: 18rem;
		top: 10%;
		right: 18%;
		animation: floatOrb 16s ease-in-out infinite;
	}

	.orb-two {
		width: 12rem;
		height: 12rem;
		bottom: 16%;
		right: 8%;
		animation: floatOrb 14s ease-in-out infinite reverse;
	}

	.orb-three {
		width: 9rem;
		height: 9rem;
		top: 48%;
		left: 10%;
		animation: floatOrb 18s ease-in-out infinite;
	}

	.grid {
		inset: 0;
		background-image:
			linear-gradient(rgba(216, 216, 216, 0.04) 1px, transparent 1px),
			linear-gradient(90deg, rgba(216, 216, 216, 0.04) 1px, transparent 1px);
		background-size: 90px 90px;
		mask-image: radial-gradient(circle at center, black 18%, transparent 80%);
		opacity: 0.35;
		animation: pulseGrid 12s ease-in-out infinite;
	}

	.glow-ring {
		width: 52rem;
		height: 52rem;
		top: 50%;
		left: 58%;
		transform: translate(-50%, -50%);
		border-radius: 50%;
		border: 1px solid rgba(80, 203, 199, 0.12);
		box-shadow:
			0 0 80px rgba(80, 203, 199, 0.08),
			inset 0 0 40px rgba(80, 203, 199, 0.05);
		animation: rotateRing 30s linear infinite;
	}

	.glow-ring::before,
	.glow-ring::after {
		content: "";
		position: absolute;
		inset: 12%;
		border-radius: inherit;
		border: 1px solid rgba(216, 216, 216, 0.06);
	}

	.glow-ring::after {
		inset: 24%;
		border-color: rgba(77, 124, 255, 0.1);
	}

	.particle-field {
		position: absolute;
		inset: 0;
	}

	.particle {
		width: 0.45rem;
		height: 0.45rem;
		border-radius: 50%;
		background: rgba(216, 216, 216, 0.85);
		box-shadow: 0 0 12px rgba(216, 216, 216, 0.6);
		opacity: 0;
		animation: twinkle 7s ease-in-out infinite;
	}

	.particle::after {
		content: "";
		position: absolute;
		top: 50%;
		left: 50%;
		width: 3.5rem;
		height: 1px;
		background: linear-gradient(90deg, rgba(216, 216, 216, 0.5), transparent);
		transform: translate(0, -50%);
	}

	.particle-one {
		top: 18%;
		left: 62%;
		animation-delay: 0s;
	}

	.particle-two {
		top: 28%;
		left: 78%;
		animation-delay: 1.2s;
	}

	.particle-three {
		top: 58%;
		left: 70%;
		animation-delay: 2.4s;
	}

	.particle-four {
		top: 72%;
		left: 54%;
		animation-delay: 3.6s;
	}

	.particle-five {
		top: 38%;
		left: 18%;
		animation-delay: 1.8s;
	}

	.particle-six {
		top: 80%;
		left: 24%;
		animation-delay: 4.2s;
	}

	@keyframes driftOne {
		from {
			transform: translate3d(0, 0, 0) scale(1);
		}
		to {
			transform: translate3d(5rem, 3rem, 0) scale(1.08);
		}
	}

	@keyframes driftTwo {
		from {
			transform: translate3d(0, 0, 0) scale(1);
		}
		to {
			transform: translate3d(-4rem, -3rem, 0) scale(1.12);
		}
	}

	@keyframes driftThree {
		from {
			transform: translate3d(0, 0, 0) scale(1);
		}
		to {
			transform: translate3d(2rem, -4rem, 0) scale(1.06);
		}
	}

	@keyframes floatOrb {
		0%,
		100% {
			transform: translate3d(0, 0, 0);
		}
		50% {
			transform: translate3d(0, -1rem, 0);
		}
	}

	@keyframes pulseGrid {
		0%,
		100% {
			opacity: 0.2;
			transform: scale(1);
		}
		50% {
			opacity: 0.35;
			transform: scale(1.04);
		}
	}

	@keyframes rotateRing {
		from {
			transform: translate(-50%, -50%) rotate(0deg);
		}
		to {
			transform: translate(-50%, -50%) rotate(360deg);
		}
	}

	@keyframes twinkle {
		0%,
		100% {
			opacity: 0;
			transform: translate3d(-1rem, 0.6rem, 0) scale(0.7);
		}
		25%,
		75% {
			opacity: 0.75;
		}
		50% {
			opacity: 1;
			transform: translate3d(1.1rem, -0.6rem, 0) scale(1);
		}
	}

	@media (max-width: 1300px) {
		.mesh-one {
			width: 30rem;
			height: 30rem;
			left: -12rem;
		}

		.glow-ring {
			width: 38rem;
			height: 38rem;
			left: 50%;
		}
	}

	@media (max-width: 768px) {
		.grid {
			background-size: 64px 64px;
			mask-image: radial-gradient(circle at center, black 10%, transparent 78%);
		}

		.orb-one {
			right: -2rem;
		}

		.orb-two {
			bottom: 24%;
			right: -1rem;
		}

		.glow-ring {
			width: 28rem;
			height: 28rem;
			top: 36%;
		}

		.particle::after {
			width: 2.2rem;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.mesh,
		.orb,
		.grid,
		.glow-ring,
		.particle {
			animation: none;
		}

		.particle {
			opacity: 0.35;
		}
	}
</style>
